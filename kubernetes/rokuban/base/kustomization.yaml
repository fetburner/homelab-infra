apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - media-pvc.yaml
  - networkpolicy.yaml
  - service.yaml

configMapGenerator:
  - name: rokuban-config
    files:
      - config.yml
