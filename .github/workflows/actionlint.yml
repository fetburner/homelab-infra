name: reviewdog

on:
  pull_request:
    paths:
      - '.github/**'

jobs:
  actionlint:
    runs-on: ubuntu-slim
    steps:
      - uses: actions/checkout@v5
      - uses: reviewdog/action-actionlint@v1
